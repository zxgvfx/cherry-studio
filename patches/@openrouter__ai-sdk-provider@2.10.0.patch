diff --git a/dist/index.d.mts b/dist/index.d.mts
index 0055af0c73ad638a59df92fa5d46edc7995da151..e2923dfdd124505790ecc628c5579a2bf0532703 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -398,6 +398,7 @@ type OpenRouterProviderOptions = {
      * help OpenRouter to monitor and detect abuse.
      */
     user?: string;
+    strictJsonSchema?: boolean;
 };
 type OpenRouterSharedSettings = OpenRouterProviderOptions & {
     /**
@@ -732,6 +733,22 @@ type WebSearchToolArgs = {
     searchPrompt?: string;
     /** Search engine to use: 'auto', 'native', or 'exa' */
     engine?: 'auto' | Engine;
+    /** Restrict results to these domains (→ allowed_domains). Mutually exclusive with excludedDomains on some engines. */
+    allowedDomains?: string[];
+    /** Exclude results from these domains (→ excluded_domains). */
+    excludedDomains?: string[];
+};
+type WebFetchToolArgs = {
+    /** Fetch engine to use: 'auto', 'native', 'exa', 'openrouter', 'firecrawl', or 'parallel' */
+    engine?: 'auto' | 'native' | 'exa' | 'openrouter' | 'firecrawl' | 'parallel' | (string & {});
+    /** Maximum fetches per request (→ max_uses). */
+    maxUses?: number;
+    /** Truncate fetched content to this many approximate tokens (→ max_content_tokens). */
+    maxContentTokens?: number;
+    /** Only fetch from these domains (→ allowed_domains). */
+    allowedDomains?: string[];
+    /** Never fetch from these domains (→ blocked_domains). */
+    blockedDomains?: string[];
 };
 
 interface OpenRouterProvider extends ProviderV3 {
@@ -774,6 +791,12 @@ interface OpenRouterProvider extends ProviderV3 {
          * @see https://openrouter.ai/docs/guides/features/server-tools/web-search
          */
         webSearch: ProviderToolFactory<unknown, WebSearchToolArgs>;
+        /**
+         * Creates an OpenRouter web fetch server tool.
+         *
+         * @see https://openrouter.ai/docs/guides/features/server-tools/web-fetch
+         */
+        webFetch: ProviderToolFactory<unknown, WebFetchToolArgs>;
     };
 }
 interface OpenRouterProviderSettings {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 0055af0c73ad638a59df92fa5d46edc7995da151..e2923dfdd124505790ecc628c5579a2bf0532703 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -398,6 +398,7 @@ type OpenRouterProviderOptions = {
      * help OpenRouter to monitor and detect abuse.
      */
     user?: string;
+    strictJsonSchema?: boolean;
 };
 type OpenRouterSharedSettings = OpenRouterProviderOptions & {
     /**
@@ -732,6 +733,22 @@ type WebSearchToolArgs = {
     searchPrompt?: string;
     /** Search engine to use: 'auto', 'native', or 'exa' */
     engine?: 'auto' | Engine;
+    /** Restrict results to these domains (→ allowed_domains). Mutually exclusive with excludedDomains on some engines. */
+    allowedDomains?: string[];
+    /** Exclude results from these domains (→ excluded_domains). */
+    excludedDomains?: string[];
+};
+type WebFetchToolArgs = {
+    /** Fetch engine to use: 'auto', 'native', 'exa', 'openrouter', 'firecrawl', or 'parallel' */
+    engine?: 'auto' | 'native' | 'exa' | 'openrouter' | 'firecrawl' | 'parallel' | (string & {});
+    /** Maximum fetches per request (→ max_uses). */
+    maxUses?: number;
+    /** Truncate fetched content to this many approximate tokens (→ max_content_tokens). */
+    maxContentTokens?: number;
+    /** Only fetch from these domains (→ allowed_domains). */
+    allowedDomains?: string[];
+    /** Never fetch from these domains (→ blocked_domains). */
+    blockedDomains?: string[];
 };
 
 interface OpenRouterProvider extends ProviderV3 {
@@ -774,6 +791,12 @@ interface OpenRouterProvider extends ProviderV3 {
          * @see https://openrouter.ai/docs/guides/features/server-tools/web-search
          */
         webSearch: ProviderToolFactory<unknown, WebSearchToolArgs>;
+        /**
+         * Creates an OpenRouter web fetch server tool.
+         *
+         * @see https://openrouter.ai/docs/guides/features/server-tools/web-fetch
+         */
+        webFetch: ProviderToolFactory<unknown, WebFetchToolArgs>;
     };
 }
 interface OpenRouterProviderSettings {
diff --git a/dist/index.js b/dist/index.js
index 3c6c1b100611983d32172288e9e40a1e6736320d..4f53a6074e3f54b7bf862712d8064b8738b1834c 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -2585,7 +2585,8 @@ var OpenRouterProviderOptionsSchema = import_v43.z.object({
     // (e.g., a future format not yet in the enum) is individually dropped
     // rather than causing the entire array to fail parsing.
     reasoning_details: ReasoningDetailArraySchema.optional(),
-    annotations: import_v43.z.array(FileAnnotationSchema).optional()
+    annotations: import_v43.z.array(FileAnnotationSchema).optional(),
+    strictJsonSchema: import_v43.z.boolean().optional()
   }).optional()
 }).optional();
 
@@ -3548,7 +3549,8 @@ var OpenRouterChatLanguageModel = class {
     responseFormat,
     topK,
     tools,
-    toolChoice
+    toolChoice,
+    providerOptions
   }) {
     var _a16, _b16, _c, _d;
     const baseArgs = __spreadValues(__spreadValues({
@@ -3607,7 +3609,8 @@ var OpenRouterChatLanguageModel = class {
             function: {
               name: tool2.name,
               description: tool2.description,
-              parameters: tool2.inputSchema
+              parameters: tool2.inputSchema,
+              strict: providerOptions?.openrouter?.strictJsonSchema
             }
           }, eagerInputStreaming != null && {
             eager_input_streaming: eagerInputStreaming
@@ -3627,7 +3630,7 @@ var OpenRouterChatLanguageModel = class {
     var _b16, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v;
     const providerOptions = options.providerOptions || {};
     const openrouterOptions = providerOptions.openrouter || {};
-    const _a16 = openrouterOptions, { cacheControl } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl"]);
+    const _a16 = openrouterOptions, { cacheControl, strictJsonSchema: _strictJsonSchema } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl", "strictJsonSchema"]);
     const args = __spreadValues(__spreadValues(__spreadValues({}, this.getArgs(options)), restOpenrouterOptions), cacheControl != null && !("cache_control" in restOpenrouterOptions) ? { cache_control: cacheControl } : {});
     const { value: responseValue, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -3822,7 +3825,7 @@ var OpenRouterChatLanguageModel = class {
     var _b16;
     const providerOptions = options.providerOptions || {};
     const openrouterOptions = providerOptions.openrouter || {};
-    const _a16 = openrouterOptions, { cacheControl } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl"]);
+    const _a16 = openrouterOptions, { cacheControl, strictJsonSchema: _strictJsonSchema } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl", "strictJsonSchema"]);
     const args = __spreadValues(__spreadValues(__spreadValues({}, this.getArgs(options)), restOpenrouterOptions), cacheControl != null && !("cache_control" in restOpenrouterOptions) ? { cache_control: cacheControl } : {});
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4542,7 +4545,7 @@ var OpenRouterCompletionLanguageModel = class {
   async doGenerate(options) {
     var _a16, _b16, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q;
     const providerOptions = options.providerOptions || {};
-    const openrouterOptions = providerOptions.openrouter || {};
+    const { strictJsonSchema: _strictJsonSchema, ...openrouterOptions } = providerOptions.openrouter || {};
     const args = __spreadValues(__spreadValues({}, this.getArgs(options)), openrouterOptions);
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4617,7 +4620,7 @@ var OpenRouterCompletionLanguageModel = class {
   }
   async doStream(options) {
     const providerOptions = options.providerOptions || {};
-    const openrouterOptions = providerOptions.openrouter || {};
+    const { strictJsonSchema: _strictJsonSchema, ...openrouterOptions } = providerOptions.openrouter || {};
     const args = __spreadValues(__spreadValues({}, this.getArgs(options)), openrouterOptions);
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4895,7 +4898,8 @@ var OpenRouterImageResponseSchema = import_v49.z.object({
   created: import_v49.z.number().optional(),
   data: import_v49.z.array(
     import_v49.z.object({
-      b64_json: import_v49.z.string()
+      b64_json: import_v49.z.string(),
+      media_type: import_v49.z.string().optional()
     }).passthrough()
   ),
   usage: import_v49.z.object({
@@ -4964,7 +4968,9 @@ var OpenRouterImageModel = class {
         message: "No images in response"
       });
     }
-    const images = responseValue.data.map((item) => item.b64_json);
+    const images = responseValue.data.map(
+      (item) => item.media_type ? `data:${item.media_type};base64,${item.b64_json}` : item.b64_json
+    );
     const usage = responseValue.usage ? {
       inputTokens: responseValue.usage.prompt_tokens,
       outputTokens: responseValue.usage.completion_tokens,
@@ -5011,6 +5017,18 @@ var webSearch = createProviderToolFactory({
   id: "openrouter.web_search",
   inputSchema: webSearchInputSchema
 });
+var webFetchInputSchema = import_v410.z.object({
+  /** Fetched page content returned by the server tool */
+  url: import_v410.z.string().optional(),
+  title: import_v410.z.string().optional(),
+  content: import_v410.z.string().optional(),
+  status: import_v410.z.string().optional(),
+  error: import_v410.z.string().optional()
+});
+var webFetch = createProviderToolFactory({
+  id: "openrouter.web_fetch",
+  inputSchema: webFetchInputSchema
+});
 
 // src/utils/remove-undefined.ts
 function removeUndefinedEntries(record) {
@@ -5324,7 +5342,8 @@ function createOpenRouter(options = {}) {
   provider.imageModel = createImageModel;
   provider.videoModel = createVideoModel;
   provider.tools = {
-    webSearch
+    webSearch,
+    webFetch
   };
   return provider;
 }
diff --git a/dist/index.mjs b/dist/index.mjs
index 6320306870c5f3ce5439f068b32b2848f6cb0c7e..6dd871944a3ccaaa36a7bd68c652cbd3f5fcc3e4 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -2552,7 +2552,8 @@ var OpenRouterProviderOptionsSchema = z3.object({
     // (e.g., a future format not yet in the enum) is individually dropped
     // rather than causing the entire array to fail parsing.
     reasoning_details: ReasoningDetailArraySchema.optional(),
-    annotations: z3.array(FileAnnotationSchema).optional()
+    annotations: z3.array(FileAnnotationSchema).optional(),
+    strictJsonSchema: z3.boolean().optional()
   }).optional()
 }).optional();
 
@@ -3515,7 +3516,8 @@ var OpenRouterChatLanguageModel = class {
     responseFormat,
     topK,
     tools,
-    toolChoice
+    toolChoice,
+    providerOptions
   }) {
     var _a16, _b16, _c, _d;
     const baseArgs = __spreadValues(__spreadValues({
@@ -3574,7 +3576,8 @@ var OpenRouterChatLanguageModel = class {
             function: {
               name: tool2.name,
               description: tool2.description,
-              parameters: tool2.inputSchema
+              parameters: tool2.inputSchema,
+              strict: providerOptions?.openrouter?.strictJsonSchema
             }
           }, eagerInputStreaming != null && {
             eager_input_streaming: eagerInputStreaming
@@ -3594,7 +3597,7 @@ var OpenRouterChatLanguageModel = class {
     var _b16, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v;
     const providerOptions = options.providerOptions || {};
     const openrouterOptions = providerOptions.openrouter || {};
-    const _a16 = openrouterOptions, { cacheControl } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl"]);
+    const _a16 = openrouterOptions, { cacheControl, strictJsonSchema: _strictJsonSchema } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl", "strictJsonSchema"]);
     const args = __spreadValues(__spreadValues(__spreadValues({}, this.getArgs(options)), restOpenrouterOptions), cacheControl != null && !("cache_control" in restOpenrouterOptions) ? { cache_control: cacheControl } : {});
     const { value: responseValue, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -3789,7 +3792,7 @@ var OpenRouterChatLanguageModel = class {
     var _b16;
     const providerOptions = options.providerOptions || {};
     const openrouterOptions = providerOptions.openrouter || {};
-    const _a16 = openrouterOptions, { cacheControl } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl"]);
+    const _a16 = openrouterOptions, { cacheControl, strictJsonSchema: _strictJsonSchema } = _a16, restOpenrouterOptions = __objRest(_a16, ["cacheControl", "strictJsonSchema"]);
     const args = __spreadValues(__spreadValues(__spreadValues({}, this.getArgs(options)), restOpenrouterOptions), cacheControl != null && !("cache_control" in restOpenrouterOptions) ? { cache_control: cacheControl } : {});
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4509,7 +4512,7 @@ var OpenRouterCompletionLanguageModel = class {
   async doGenerate(options) {
     var _a16, _b16, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q;
     const providerOptions = options.providerOptions || {};
-    const openrouterOptions = providerOptions.openrouter || {};
+    const { strictJsonSchema: _strictJsonSchema, ...openrouterOptions } = providerOptions.openrouter || {};
     const args = __spreadValues(__spreadValues({}, this.getArgs(options)), openrouterOptions);
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4584,7 +4587,7 @@ var OpenRouterCompletionLanguageModel = class {
   }
   async doStream(options) {
     const providerOptions = options.providerOptions || {};
-    const openrouterOptions = providerOptions.openrouter || {};
+    const { strictJsonSchema: _strictJsonSchema, ...openrouterOptions } = providerOptions.openrouter || {};
     const args = __spreadValues(__spreadValues({}, this.getArgs(options)), openrouterOptions);
     const { value: response, responseHeaders } = await postJsonToApi({
       url: this.config.url({
@@ -4862,7 +4865,8 @@ var OpenRouterImageResponseSchema = z10.object({
   created: z10.number().optional(),
   data: z10.array(
     z10.object({
-      b64_json: z10.string()
+      b64_json: z10.string(),
+      media_type: z10.string().optional()
     }).passthrough()
   ),
   usage: z10.object({
@@ -4931,7 +4935,9 @@ var OpenRouterImageModel = class {
         message: "No images in response"
       });
     }
-    const images = responseValue.data.map((item) => item.b64_json);
+    const images = responseValue.data.map(
+      (item) => item.media_type ? `data:${item.media_type};base64,${item.b64_json}` : item.b64_json
+    );
     const usage = responseValue.usage ? {
       inputTokens: responseValue.usage.prompt_tokens,
       outputTokens: responseValue.usage.completion_tokens,
@@ -4978,6 +4984,18 @@ var webSearch = createProviderToolFactory({
   id: "openrouter.web_search",
   inputSchema: webSearchInputSchema
 });
+var webFetchInputSchema = z11.object({
+  /** Fetched page content returned by the server tool */
+  url: z11.string().optional(),
+  title: z11.string().optional(),
+  content: z11.string().optional(),
+  status: z11.string().optional(),
+  error: z11.string().optional()
+});
+var webFetch = createProviderToolFactory({
+  id: "openrouter.web_fetch",
+  inputSchema: webFetchInputSchema
+});
 
 // src/utils/remove-undefined.ts
 function removeUndefinedEntries(record) {
@@ -5291,7 +5309,8 @@ function createOpenRouter(options = {}) {
   provider.imageModel = createImageModel;
   provider.videoModel = createVideoModel;
   provider.tools = {
-    webSearch
+    webSearch,
+    webFetch
   };
   return provider;
 }
